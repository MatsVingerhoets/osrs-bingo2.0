Cross-feature repositories live here.

Current scope:

- auth user persistence
- events, teams, boards, and completions persistence

Repositories should encapsulate query logic and depend on shared types from `src/models` plus infrastructure from `src/lib`.
