Shared application models live here.

Current scope:

- auth-facing app models
- shared database table and row types

Feature modules can define local view models when needed, but cross-feature types should live in this folder.
