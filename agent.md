Project notes
-------------
- Do not use Yarn in this project; prefer npm for any Node-based tool installs (e.g., markdown-preview.nvim build uses npm).
- Never commit or push code without explicit user approval first.
